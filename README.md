🚀 NOTES API – Serverless Backend on AWS

A fully serverless notes application backend using Lambda, API Gateway HTTP API, DynamoDB, IAM, and Python.

This project implements a production-grade serverless REST API that stores and retrieves notes from DynamoDB using an AWS Lambda backend. This project was built as part of my AWS Solutions Architect learning journey and demonstrates real-world cloud architecture, IAM security, API design, and serverless development.

🏗️ Architecture Diagram
Client → API Gateway HTTP API → Lambda (Python) → DynamoDB Table (notes-app)


AWS Services used:

Amazon API Gateway (HTTP API)

AWS Lambda (Python 3.12)

Amazon DynamoDB

AWS IAM (execution role)

CloudWatch Logs

The architecture is designed to be fully serverless, autoscaling, and cost-efficient.

📦 Project Structure
notes-app-backend/
├── src/
│   └── handler.py          # Main Lambda function
├── function.zip            # Deployment artifact
├── README.md               # Documentation
├── payload.json            # Example test payloads
└── .gitignore

📝 DynamoDB Table Schema

Table Name: notes-app
Partition Key: noteId (String)

Each note item contains:

Field	Type	Description
noteId	String	UUID generated by Lambda
userId	String	Hardcoded for demo (test-user-123)
title	String	Note title
content	String	Note body
createdAt	String	ISO timestamp
updatedAt	String	ISO timestamp
🔥 REST API Endpoints
Method	Path	Description
GET	/notes	Return all notes
POST	/notes	Create a new note
📄 Example Requests
✔️ Get all notes
curl -X GET https://YOUR_API_ID.execute-api.us-east-1.amazonaws.com/notes


Example Response:

{
  "items": [
    {
      "noteId": "1234",
      "title": "Hello World",
      "content": "From Lambda",
      "createdAt": "...",
      "updatedAt": "..."
    }
  ]
}

✔️ Create a note
curl -X POST \
  -H "Content-Type: application/json" \
  -d '{"title":"My API Note","content":"Created from curl"}' \
  https://YOUR_API_ID.execute-api.us-east-1.amazonaws.com/notes


Example Response:

{
  "noteId": "abcd-1234",
  "title": "My API Note",
  "content": "Created from curl",
  "createdAt": "...",
  "updatedAt": "..."
}

🧠 Lambda Handler (Python)

Your Lambda handles routing based on:

route = event.get("requestContext", {}).get("http", {}).get("path", "")
method = event.get("requestContext", {}).get("http", {}).get("method", "")


Supported:

GET /notes

POST /notes

🚀 Deployment Instructions
1. Zip your code

From inside the project:

zip -r function.zip src/


or use your existing zip.

2. Upload to Lambda

AWS Console → Lambda → notes-app-handler:

Code → Upload from → .zip file


Click Deploy.

3. Test in Lambda Console

Use a test event like:

{
  "httpMethod": "POST",
  "body": "{\"title\":\"Lambda Test\",\"content\":\"Hello\"}"
}

4. Deploy API Gateway

API Gateway → Your API → Stages → $default
Automatic deployment should be enabled.

🧪 Local Testing (Python)

You can simulate a POST event locally:

python3 - << 'EOF'
from src.handler import lambda_handler
print(lambda_handler({
    "requestContext": {"http": {"method": "POST", "path": "/notes"}},
    "body": '{"title":"Local Test","content":"From local Python"}'
}, {}))
EOF

🎯 What This Project Demonstrates (Great for Employers)

Real API Gateway integration

Lambda routing logic

DynamoDB CRUD operations

IAM Role design for DynamoDB access

CloudWatch debugging

REST API best-practices

Infrastructure architecture thinking

This is exactly the type of project AWS Solutions Architect Associate candidates build to understand serverless design.

📌 Future Enhancements (Optional)

These are perfect improvements you can add later:

Add /notes/{id} GET, PUT, DELETE

Add user authentication (Cognito)

Add CORS + React frontend

Add Terraform or CloudFormation IaC

Add pagination

Add request validation

🧑‍💻 Author

Joseph McCoy (jmac052002)
AWS Student • Python Developer • DevOps Learner
GitHub: https://github.com/jmac052002
